# Decision Core Evidence Companion

## Purpose

This file is the human-facing companion to
[decision-core-evidence.ttl](/Users/lou/oracle/decision-core-evidence.ttl).
The Turtle file is the source of truth. This document explains what the named
instances mean and why they matter.

## Canonical Source Lineage

The canonical precursor lineage comes from:

- repository root: `/Volumes/lemon/gemini/ontology-framework`
- main proto-core cluster: April 2025
- alternatives cluster: July 2025

The model treats those artifacts as first-class evidence rather than as loose
references in notes.

## Evidence Clusters

### April 2025 Proto-Core Cluster

This is the canonical formation point for the current model. It is where the
repository begins to behave like a decision-assurance system instead of a set of
separate ontology experiments.

Artifacts represented in this cluster:

- `oracle-oci-discovery-ontology.ttl`
- `ontologist.prompt`
- `guidance/modules/validation.ttl`
- `issue_planning_process.md`
- `models/project_maintenance.ttl`
- `session.ttl`

Interpretation:

- ontology exists before implementation
- validation exists before trust
- governance exists before scale
- session and workflow state are modeled, not improvised

### July 2025 Alternatives Cluster

This is the anti-brute-force correction layer.

Artifact represented in this cluster:

- `property_graph_confidence_assessment.md`

Interpretation:

- the structured system was already fast enough
- performance claims required measurement
- targeted augmentation was acceptable
- indiscriminate scaling was not the governing instinct

## Recovered Roles

The evidence layer uses recovered roles to say what each artifact contributes.

- `Seed Ontology`: early domain structure
- `Process Doctrine`: ontology-first operating rules
- `Validation Core`: policy and decision traceability
- `Governance Kernel`: metrics, change, maintenance linkage
- `Workflow Model`: issue-to-plan and dependency process
- `Session State Model`: active work represented structurally
- `Anti-Brute-Force Analysis`: explicit skepticism of scale-for-scale's-sake

## Why This Matters

The evidence layer supports a concrete lineage claim:

`structure first -> validate continuously -> trace decisions -> govern changes`

That claim is not just asserted in prose. It is backed by:

- repository provenance
- branch lineage
- commit anchors
- file identities
- interpreted roles

## Trusted Operational Overlay

The core lineage is strengthened by trusted local skill patterns captured in
[decision-core-workflows.ttl](/Users/lou/oracle/decision-core-workflows.ttl).

The two initial patterns are:

- `eudorus-control-tower`
- `daily-ops-story-report`

These patterns confirm that the model is not only conceptual. It also requires:

`preflight -> classification -> gated execution -> evidence-backed report`

## Current Constraint

This evidence set is sufficient for the first stable version of the ontology.
It is not yet meant to be exhaustive across every old repository. Additional
archaeology should be added only if it changes the model materially, not just
because another related note exists.
